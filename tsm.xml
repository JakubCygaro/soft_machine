<graph>
    <memory name="MEM">
        <memset>
            1 2 3 4 5
        </memset>
    </memory>
    <cpu name="CPU">
        <code>
            <wait on="TRIG-CPU" into="R0"/>
            <load from="CPU-MEM" into="R0" at="1"/>
            <add dest="R0" src="R2"/>
            <send from="R0" on="CPU-OTHER"/>
            <restart/>
        </code>
    </cpu>
    <passthrough name="CPU-MEM" from="MEM" to="CPU"/>
    <custom>
        <state>
            (def text "Hello, world!")
        </state>
        <logic>
            (send
                :to "SOME-NODE"
                :msg text)
            (let (
                sender-msg (recv))
            (
                (display (second sender-msg))
            ))
        </logic>
    </custom>
</graph>
